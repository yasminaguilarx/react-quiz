


const Button = () => {

    const handleClick = () => {
        // run the toggleLogInProp
    }

    return (
        
        <h1>create a button and add handleClick callback </h1>
        
    )
}

export default Button